ISSUES

Reviewed revision `c807a841e161e989ed5e0741fb336b0dedb88115` independently against `origin/main...HEAD`, the design, implementation plan, changed skills/schemas/renderer, Gauntlet fixture/harness/comparator/tests, and retained runtime/full-evidence artifacts. Findings 1–7 are release-blocking quality issues. Finding 8 is non-blocking report usability debt. The separate native-acceptance and `NOT_PROVEN` boundaries follow the findings.

1. **Critical — release blocker — a failed run can be exported and labeled `PASS`.** File/line: `tests/skill_product/gauntlet/redact_evidence.py:28-65`. Evidence: `export()` selects its allowlist and writes the packet from the caller-supplied `status`; it never reads `acceptance-result.json` to require that authoritative status to match. Against the retained runtime run, whose authoritative status is `FAIL`, calling `export(..., "PASS")` succeeded, returned `export packet status=PASS`, copied an acceptance result still saying `FAIL`, and exported the zero-byte controller `run.log`. This contradicts the acceptance record's statement that the harness forbids PASS export on FAIL and permits misleading durable evidence. Smallest repair: parse and validate the source acceptance result, require exact equality with the requested export status, permit `PASS` only for authoritative `PASS`, require every selected file to be non-empty, and add negative tests for FAIL/REVIEW_REQUIRED relabeling and empty logs.

2. **Critical — release blocker — finalization can issue `PASS` without validating either canonical artifact, evidence references, or cross-artifact equality.** File/line: `tests/skill_product/gauntlet/run_acceptance.py:205-248`; misleading positive coverage at `tests/skill_product/test_gauntlet_acceptance.py:77-100,136-154`; under-specified result contract at `tests/skill_product/gauntlet/acceptance-result.schema.json:1-19` and `tests/skill_product/gauntlet/acceptance_result.py:13-29`. Evidence: `finalize()` checks only that three files exist and are non-empty, parses only `report-input.json`, ignores the contents of `readiness-ledger.json`, never invokes either installed schema or `frontier_validation.py`, never verifies evidence paths, and never proves that the two frontiers/findings match. The passing harness fixture at lines 77-83 deliberately writes `readiness-ledger.json` with a list-shaped frontier and nonexistent evidence while the comparator consumes a separate legacy report packet; `test_finalize_completed_pass_is_atomic_and_cleans_controller` still expects `PASS`. The acceptance-result schema also omits the design-required run ID, platform, timestamps, artifact digests/validation states, claimed/derived closure, mismatches, review reasons, exit code, and cleanup state. Smallest repair: make finalization schema-validate both JSON artifacts, run cross-field/evidence-root validation, require byte/semantic equality of the mandated frontier and exact finding lineage, verify the HTML was rendered from that report input, and expand plus actually validate the acceptance result against its declared schema before an atomic final write.

3. **High — release blocker — the current-run artifact contract is internally contradictory and the comparator rejects the exact shape the skill mandates.** File/line: `plugins/shipworthy/skills/ship-readiness-orchestrator/SKILL.md:226-230`, `plugins/shipworthy/skills/ship-readiness-orchestrator/references/schemas/report-input.schema.json:15-56`, `tests/skill_product/gauntlet/run_acceptance.py:184-202`. Evidence: the skill requires the same top-level `path_frontier` in both final JSON files, while `report-input.schema.json` has `additionalProperties: false` and permits the frontier only through `source_ledger`. `comparison_view()` likewise projects only `source_ledger.path_frontier`; otherwise it expects legacy top-level `rows`. The retained runtime report follows both shapes and fails the report schema for the extra top-level property; the retained full-evidence report follows the explicit top-level instruction and is reduced by the comparator to zero rows. This is the recorded full-evidence C failure, but it is also a deterministic implementation defect, not merely an acceptance limitation. Smallest repair: choose one canonical report representation consistent with the design's `source_ledger` model, align skill/prompt/schema/renderer/comparator to it, enforce equality with the ledger, and add a finalize-level test using the exact production artifact shape.

4. **High — release blocker — the comparator can return `PASS` for unexercised, unevidenced, invented material controls.** File/line: `tests/skill_product/gauntlet/compare_agent_result.py:187-278`; the behavior is explicitly blessed at `tests/skill_product/test_gauntlet_comparator.py:187-204`. Evidence: the comparator checks only that `evidence_refs` is a non-empty list, not that references resolve or meet `minimum_evidence_class`; it does not require `attempt_count >= 1`; and lines 262-271 suppress every unmatched material row whose route is in `supporting_routes`. A complete packet modified so every row had `attempt_count: 0` and `evidence_refs: ["does-not-exist"]`, plus an invented covered `/projects` control, returned `PASS`, with zero unexpected rows and no reasons. This violates execution, honesty, and unexpected-row review requirements. Smallest repair: validate the canonical frontier before comparison, enforce each oracle item's evidence class and safe-control attempt requirement, and replace route-wide suppression with exact, versioned supporting semantic identities; every other unmatched material row must be `REVIEW_REQUIRED`.

5. **High — release blocker — the prescribed deterministic suite is red and the v1 contract changed incompatibly.** File/line: `plugins/shipworthy/skills/ship-readiness-orchestrator/references/schemas/readiness-ledger.schema.json` (new conditional Fix requirements and frontier definition), `tests/skill_product/test_ledger_contract.py:15-34,61-77`, `tests/skill_product/test_dual_render.py:43-46`, `tests/skill_product/test_independent_skills.py:15-27`. Evidence: `python3 -m unittest discover -s tests/skill_product -p 'test_*.py'` ran 171 tests and ended with 3 failures, 2 errors, and 4 skips. Existing approved v1 ledger/report fixtures now fail because every Fix finding gained mandatory fields without a schema-version change; approved schema hashes differ; the legacy renderer's immutable HTML digest changed; and the independent-skill reference checker treats `readiness-ledger.schema.json#/$defs/path_frontier` as a nonexistent filename. The focused 100-test Gauntlet suite and direct legacy scripts pass, so the focused selection masks these regressions. The plan's standalone validator command also exits 1 because it passes a report wrapper to a tool that expects a frontier object, and its forbidden-network grep flags the harness's documented localhost `urllib.request` health check. Smallest repair: decide and implement an explicit compatibility/versioning policy, migrate or preserve v1 fixtures accordingly, fix fragment-aware local-reference checking, deliberately re-baseline renderer output only after review, and correct the final verification commands so the exact documented gate is green.

6. **High — release blocker — the frozen acceptance report overstates canonical proof and its B classifications are not technically supported.** File/line: `docs/phase0/evidence/gauntlet-post-fix-acceptance.md:20-23,27-58`. Evidence: direct validation of the retained artifacts found runtime `report-input.json` schema-invalid because of the forbidden top-level frontier, and its frontier fails repository validation due an orphan feature and observation pass IDs absent from `discovery_passes`. Full-evidence `report-input.json` is not a report-input schema document at all (it lacks required `schema_name`, `schema_version`, `producer`, `report_input_id`, and `source_ledger`), and its frontier fails evidence resolution under the repository validator. Therefore “Canonical artifact proof” and “No full-evidence A or B miss was established” are too strong: both modes have current-artifact/proof failures (B), regardless of whether their narrative observations cover the fixture. The listed `run.log` digests are for `agent-evidence/run.log`; the authoritative exporter uses the root `run.log`, which is empty in both retained runs and has SHA-256 `e3b0c442...`, so the evidence path is ambiguous and does not identify the canonical six-file export set. Smallest repair: reclassify the schema/frontier/evidence failures as B, qualify narrative discoveries separately from canonical proof, give exact retained paths for every digest, and record the deterministic-suite failures and absence of any PASS/waiver.

7. **High — release blocker — `shipworthy-semantic-v1` is not the specified deterministic normalization.** File/line: `tests/skill_product/gauntlet/compare_agent_result.py:20-29`, mirrored by `tests/skill_product/support/frontier_validation.py:45-49,86-88` and the ASCII-only schema pattern in `readiness-ledger.schema.json`'s `FrontierRow.semantic_key`. Evidence: the design requires preservation of Unicode letters/numbers and percent-decoded, segment-normalized routes. The implementation uses `[^a-z0-9]+`, so `Café` becomes `caf` and `東京` becomes the empty token; `normalize_route('/caf%C3%A9//résumé/')` returns `/caf%c3%a9/résumé` rather than a decoded, token-normalized route. Comparator, validator, and schema therefore disagree with the published version-1 identity contract and can collide or reject legitimate international product surfaces. Smallest repair: centralize one Unicode-category-aware normalizer, percent-decode then normalize each route segment, use its grammar consistently in schema/validator/comparator, and add accented/CJK/encoded-path collision tests before retaining the v1 label (or version the corrected algorithm).

8. **Medium — non-blocking usability debt — non-closed Product Coverage states are presented with a success-colored chip and feature summaries omit the most important gap.** File/line: `plugins/shipworthy/skills/ship-readiness-orchestrator/scripts/render_report.py:256-325,690-692`. Evidence: all closure states use the same green `#34D399` chip, including `incomplete`, `single_source`, `blocked`, and `static_only`. Feature rows show only surface/control counts and raw status, while the design requires what was checked, outcome, and the most important gap. The HTML is otherwise self-contained, escaped, action-first, bounded, and readable; default JavaScript is absent. Smallest repair: map chip color/text to closure state and expose one bounded terminal reason/evidence gap per feature, with tests for every non-closed state and print/mobile rendering.

## Acceptance limitations and proof boundaries

- The runtime-only frozen run is independently confirmed nonpassing. Its admin/mobile Invite row is `sampled_with_justification`, `attempt_count: 0`, while the oracle requires `covered`; that A classification is supported. Its reported C aliases and D extras may be useful diagnostic classifications, but they do not offset the A miss or the unrecorded B artifact failures.
- The full-evidence run is also nonpassing. The zero-row comparator outcome is caused by Finding 3, and the retained observations plausibly support the stated semantic-alias C diagnosis and two D extras. However, invalid report/frontier proof means “no B miss” is not established. Neither mode has an individual PASS, durable PASS export, or recorded human waiver; under the plan this independently blocks release.
- Filesystem containment remains `NOT_PROVEN`, and oracle blindness is procedural only. Those are correctly disclosed limitations rather than behavioral failures, but no wording may upgrade them to technical isolation. The fixture binds to `127.0.0.1`, reset requires a random token, installed skill scripts contain no new provider dispatch/client network/install behavior, and no additional security blocker was found beyond the false-PASS/export and validation boundaries above.
- Verification summary: focused Gauntlet/frontier/report suite 100/100 passed; full discovery suite failed (171 run, 3 failures, 2 errors, 4 skipped); four direct legacy scripts passed (139, 174, 22, and 17 checks); parity/four-skill/stdlib suite passed 16/16; compilation passed; `git diff --check` was clean. The documented standalone frontier-validation command and Gauntlet forbidden-network grep did not pass as written.
